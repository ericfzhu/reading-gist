{
  "name": "book",
  "version": "1.0.0",
  "description": "Updates a reading list gist to show currently reading books on Notion",
  "main": "index.ts",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node --files src/index.ts",
    "start:debug": "node --inspect-brk -r ts-node/register src/index.ts"
  },
  "dependencies": {
    "@notionhq/client": "^0.4.0",
    "@octokit/core": "^3.5.1",
    "dotenv": "^10.0.0"
  },
  "author": "Eric Zhu",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.10.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ericfzhu/book"
  }
}
